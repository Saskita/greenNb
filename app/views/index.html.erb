<h1>Plants 🌿</h1>
<ul>
  <% @plants.each do |plant| %>
    <li>
    <%= link_to plant.name, plant_path(plant) %>
    </li>
  <% end %>
</ul>

<p>➕ <%= link_to "Add a new plant", new_plant_path %></p>
